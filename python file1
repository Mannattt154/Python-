'''
print(f"{num:.1f}")  # 1 decimal place
print(f"{num:.2f}")  # 2 decimal place
print(f"{num:.3f}")  # 3 decimal place
'''

'''1. Write a python program to take total minutes as input and convert it into hours and remaining minutes'''
# total_minutes = int(input())
# hours  = total_minutes/60
# minutes = total_minutes%60
# print(f"{b} hours and {c} minutes")
'''Write a python program to take a 2 digit number as input and print the sum if its digits'''
# two = int(input())
# a = two//10
# c = two%10
# print(a+c)
'''Write a python  program to take an amount in rupees and calculate how many 500 and 100 notes are needed'''
# amount = int(input("Enter amount: "))
# notes_500 = amount // 500
# remaining_amount = amount % 500
# notes_100 = remaining_amount // 100
# print("500 rupee notes =", notes_500)
# print("100 rupee notes =", notes_100) 

'''Write a python program to take two inputs a and b, swap their values using a temorary variable, and print updated values.'''
a = int(input("Enter value of a:"))
b = int(input("Enter value of b:"))
c = a
a = b
b = c
print(f"After swapping: a = {a}, b = {b}")

'''Write a python program to swap two numbers without using a third variable.'''
a = int(input("enter first number:"))
b = int(input("enter second number:"))
a = a + b
b = a - b
a = a - b
print(f"After swapping: a = {a}, b = {b}")

'''Write a pg to take age as input and print True if age is between 18 and 60, otherwise print False'''
'''To take  marks of three subjects out of 100.
 Print True if the students scored at least 40 in all three sunjects and average marks are at least 50'''
'''To take an integer and print True if the number is even and False if the number is odd'''
'''o take input from the user without typecasting and multiply it by 3. Then typecast the same input to int and multiply it by 3
print both the results to show the difference'''
'''to take student details like name, roll number, CGPA, and hostel status from the user. 
Typecast them into appropriate types and print them along with their detected type'''
name = input("enter name:")
roll_no = int(input("enter roll number:"))
cgpa = float(input("Enter CGPA:"))
is_hosteller = bool(input("Are you a hosteller?"))
print("Name:", name, type(name))
print("Roll Number:", roll_no, type(roll_no))
print("CGPA:", cgpa, type(cgpa))
print("Is Hosteller:", is_hosteller, type(is_hosteller))

# write a python program to display a user_entered name followed by "Good afternoon" using input() function (use f-string)
name = input("Enter your name: ")
print(f"Good Afternoon, {name}")

# write a python program to fill the given letter template with name and date.
letter = '''
 Dear <Name>,
 You are selected!
 <Date> 
 '''

name = input("Enter name:")
date = input("Enter date:")
letter = letter.replace("<Name>", name)
letter = letter.replace("<Date>", date)
print(letter)
