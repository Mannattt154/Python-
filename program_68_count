# WAP to count how many times a particular elememts appears in a list
numbers = [10, 20, 30, 10, 40, 20, 10]
search = int(input("enter number to count:"))
count = 0
for num in numbers:
    if num == search:
        count += 1
        print("Frequency: ", count)
